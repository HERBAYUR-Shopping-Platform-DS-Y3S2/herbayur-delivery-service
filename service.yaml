apiVersion: v1
kind: Service
metadata:
  name: herbayur-delivery-service
spec:
  type: LoadBalancer
  ports:
    - protocol: TCP
      port: 5002
      targetPort: 5002
  selector:
    app: herbayur-delivery-service-app

# //service.yaml file
# -----------------------------
# services:

#   inventory:
#     git_url: https://github.com/HERBAYUR-Shopping-Platform-DS-Y3S2/herbayur-delivery-service/tree/develop
#     git_branch: develop
#     command: npm start
#     build_root: .
#     ports:
#       - container: 5002
#         http: 50
#         https: 443
#     env_vars:
#       NODE_ENV: production

# databases:  
#   - mongodb
